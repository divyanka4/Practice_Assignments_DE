# This is a Declarative Automation Bundle definition for Assignment_9_Dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Assignment_9_Dabs
  uuid: 50491328-721a-4458-8476-e6dff487cb31

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-09bdc850-08ea.cloud.databricks.com/
    variables:
      catalog: dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-af5175d7-cbe7.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/paldivya0222@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/d7f7ae0c-376f-4f1f-8586-0c41fd8c43d3/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev
      schema: prod

    run_as:
      service_principal_name: "d7f7ae0c-376f-4f1f-8586-0c41fd8c43d3"

    permissions:
      - user_name: paldivya0222@gmail.com
        level: CAN_MANAGE
      - service_principal_name: "d7f7ae0c-376f-4f1f-8586-0c41fd8c43d3"
        level: CAN_MANAGE
